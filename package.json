{
  "name": "meditation-timer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm run build:root && pnpm run build:frontend && pnpm run build:backend",
    "build:root": "pnpm install",
    "build:frontend": "cd packages/frontend && pnpm install && pnpm run build && cd ../..",
    "build:backend": "cd packages/backend && pnpm install && cd ../..",
    "start": "pnpm run start:backend",
    "start:backend": "cd packages/backend && pnpm run start && cd ../.."
  },
  "packageManager": "pnpm@9.15.0"
}
